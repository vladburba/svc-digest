feedparser>=6.0
httpx[socks]>=0.28
python-dotenv>=1.0
# Страховка под ZoneInfo("Europe/Moscow"): в текущем python:3.12-slim база зон
# есть, но Debian-slim её не гарантирует. Без зоны сервис падал бы на импорте —
# то есть дайджест бы молча пропал. 700 КБ за отсутствие такого класса отказов.
tzdata>=2024.1
